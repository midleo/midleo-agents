input {
    udp {                                                                                      
       port => 15044                                                                            
       codec => "json"                                                                          
       type => "rsyslog"                                                                        
    }
    beats {
        port => "5044"
        ssl_enabled => false
    }
}
filter {
  if [event][module] == "ibmmq" {
     json {
        source => "message"
        remove_field => ["ecs","agent","log","host","event","@timestamp","tags","fileset","input","@version","service"]
     }
     mutate { 
        add_field => { 
            "inttoken" => "${MIDLEO_INTTOKEN:}"
            "srvtype" => "ibmmq"
        } 
        rename => { 
            "[ibm_messageId]" => "[monid]" 
            "[ibm_datetime]" => "[alerttime]"
            "[ibm_serverName]" => "[appsrv]"
        }
     }
  } 
  if [type] == "ace_message" {
     json {
        source => "message"
        remove_field => ["tags","@version","loglevel","ibm_sequence"]
     }
     mutate {
        split => { "module" => "." }
        add_field => { 
            "srvid" => "${MIDLEO_SERVER_ID:}"
            "inttoken" => "${MIDLEO_INTTOKEN:}"
            "srvtype" => "ibmace"
            "monid" => "BIP%{[ibm_messageId]}"
            "appsrv" => "%{[module][0]}"
        } 
        rename => { 
            "[ibm_datetime]" => "[alerttime]"
        }
     }
  }
  if [srvtype] == "ibmiib" {
     grok {
      match => { "message" => ".*\(%{WORD:appsrv}\.%{WORD:appintsrv}\).*%{WORD:monid}:.*" }
     }
     json {
        source => "message"
        remove_field => ["procid","sysloghost"]
     }
     mutate {
        add_field => { 
            "inttoken" => "${MIDLEO_INTTOKEN:}"
        } 
        rename => { 
            "[@timestamp]" => "[alerttime]"
        }
     }
  }
}
output {
   if [srvtype] {
     http {
         url => "${MIDLEO_MONALERT_URL:http://web/pubapi/monalert}"
         http_method => "post"
         format => "json"
         content_type => "application/json;charset=UTF-8"
         connect_timeout => 10
         socket_timeout => 20
         request_timeout => 30
         retry_failed => true
     }
   }
}
